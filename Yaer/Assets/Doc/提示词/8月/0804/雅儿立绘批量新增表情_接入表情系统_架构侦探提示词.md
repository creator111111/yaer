# Cursor Agent Prompt · 雅儿立绘批量新增表情 · 接入表情系统

> **角色**：先【架构侦探】，报告通过后再定【施工员】方案  
> **日期**：2026-08-04  
> **范围**：雅儿（Yaer）村线 GoOut 大立绘 `GoOutStoryYaerPainting` 的 `Faces` 批量新表情；古莎 / Mask 接线仅作对照，**本期不改古莎、不施工 Mask 启用**  
> **本阶段**：只摸清缺口与改动面，**不施工**  
> **强对照**：`0803` 雅儿 **Happy** 轮溯源报告 + 古莎 LuoMo/LuoMo2「加枚举末尾」套路

把下面整段复制给 Cursor Agent（Agent Mode）执行。本阶段**只溯源、不改代码**。

---

## 提示词助手预梳理（侦探须核实，勿当唯一真相）

### A. Prefab 现状（截图 + `GoOutStoryYaerPainting.prefab` 已对齐）

Hierarchy：`GoOutStoryYaerPainting` → `Faces` → 下列节点（键规则：`Armor_NoHeadWear_{FaceType}`）：

| # | Faces 节点名 | 推断面部枚举名 | 相对「原雅儿 9 + Happy」 |
|---|--------------|----------------|-------------------------|
| 1 | `Armor_NoHeadWear_Unhappy` | `Unhappy` | 原有 |
| 2 | `Armor_NoHeadWear_Daze` | `Daze` | 原有 |
| 3 | `Armor_NoHeadWear_Smug` | `Smug` | 原有 |
| 4 | `Armor_NoHeadWear_Smile` | `Smile` | 原有 |
| 5 | `Armor_NoHeadWear_Surprised` | `Surprised` | 原有 |
| 6 | `Armor_NoHeadWear_Laugh` | `Laugh` | 原有 |
| 7 | `Armor_NoHeadWear_Sad` | `Sad` | 原有 |
| 8 | `Armor_NoHeadWear_VerySurprised` | `VerySurprised` | 原有 |
| 9 | `Armor_NoHeadWear_ChiBie` | `ChiBie` | 原有 |
| 10 | `Armor_NoHeadWear_ChiBie2` | `ChiBie2` | **Prefab 新增；枚举缺** |
| 11 | `Armor_NoHeadWear_ChiBie3` | `ChiBie3` | **Prefab 新增；枚举缺** |
| 12 | `Armor_NoHeadWear_GanGa` | `GanGa` | **Prefab 新增；枚举缺** |
| 13 | `Armor_NoHeadWear_Happy` | `Happy` | 0803 已接（枚举已有） |
| 14 | `Armor_NoHeadWear_RenNai` | `RenNai` | **截图红标新增；枚举缺** |
| 15 | `Armor_NoHeadWear_Angry` | `Angry` | **截图红标新增节点；枚举已有** |
| 16 | `Armor_NoHeadWear_NanGuo` | `NanGuo` | **截图红标新增；枚举缺** |
| 17 | `Armor_NoHeadWear_NanGuo2` | `NanGuo2` | **截图红标新增；枚举缺** |
| 18 | `Armor_NoHeadWear_NanGuo3` | `NanGuo3` | **截图红标新增；枚举缺** |
| 19 | `Armor_NoHeadWear_NanGuo4` | `NanGuo4` | **截图红标新增；枚举缺** |
| 20 | `Armor_NoHeadWear_ZhenJing` | `ZhenJing` | **截图红标新增；枚举缺** |
| 21 | `Armor_NoHeadWear_ZhenJing2` | `ZhenJing2` | **截图红标新增；枚举缺** |

> 说明：截图红线从 `RenNai` 画到末尾；但 **`ChiBie2` / `ChiBie3` / `GanGa` 也不在原 9+Happy 表、也不在当前枚举**，本期应一并纳入缺口盘点，勿漏。

### B. 当前 `DialogueFaceType` 对照结论（提示词助手只读，侦探复核）

**原雅儿常用 9**：`Unhappy` `Daze` `Smug` `Smile` `Surprised` `Laugh` `Sad` `VerySurprised` `ChiBie` — 枚举均有。  
**已有先例**：`Happy` — 枚举有、GoOut 节点有（0803）。  
**枚举已有、雅儿 GoOut 新挂脸**：`Angry` — **可能零改枚举即可调大立绘**（须核实键规则与绑图）。

**须追加枚举（建议名单，仅追加末尾）**：

| 候选枚举名 | Prefab 节点 | 中文猜测（待拍板） | 备注 |
|------------|-------------|-------------------|------|
| `ChiBie2` | `Armor_NoHeadWear_ChiBie2` | 吃瘪2 | 变体 |
| `ChiBie3` | `Armor_NoHeadWear_ChiBie3` | 吃瘪3 | 变体 |
| `GanGa` | `Armor_NoHeadWear_GanGa` | 尴尬 | 与古莎 `Awkward` 语义近，**勿混名** |
| `RenNai` | `Armor_NoHeadWear_RenNai` | 忍耐 | 红标 |
| `NanGuo` | `Armor_NoHeadWear_NanGuo` | 难过 | 与已有 `Sad`（苦闷/难过）语义近，**勿建议合并枚举** |
| `NanGuo2` | `Armor_NoHeadWear_NanGuo2` | 难过2 | |
| `NanGuo3` | `Armor_NoHeadWear_NanGuo3` | 难过3 | |
| `NanGuo4` | `Armor_NoHeadWear_NanGuo4` | 难过4 | |
| `ZhenJing` | `Armor_NoHeadWear_ZhenJing` | 震惊 | 与 `VerySurprised` 语义近，**勿建议合并** |
| `ZhenJing2` | `Armor_NoHeadWear_ZhenJing2` | 震惊2 | |

**不需为雅儿新加枚举**：`Angry`（已存在）。

### C. 已知契约（侦探复核）

- GoOut 大立绘键：`Armor_NoHeadWear_{faceType}`（`GoOutStoryYaerPainting`）
- 小头像：图集子图名 = 枚举英文名（`DialogueAvatarLoader` / `Avatar_Yaer_*.spriteatlas`）
- 连衣裙线 `YaerPainting`：`Dress_Crown_*` — **本期默认不扩**，只写清缺口
- 新增枚举：**只能追加在 `DialogueFaceType` 末尾**，禁止插入中间

---

```
@Assets/Doc/00_MASTER_PROMPT.md
@Assets/Project_context.md
@Assets/Doc/02_SYSTEM_SPEC.md
@Assets/Doc/执行文档/0803/雅儿立绘新增Happy表情_接入表情系统_架构溯源报告.md
@Assets/Doc/执行文档/6月/0601/对话立绘表情与图片名称对照_执行说明.md
@Assets/Doc/OPEN_QUESTIONS.md

你现在是【架构侦探】。Unity 2020.3.48f1 / C#。
禁止修改任何代码、Prefab、场景、图集、CSV、台本。只读扫描 + 写溯源报告。

---

## 背景（策划白话，已对齐截图）

1. 开发者已在 **`GoOutStoryYaerPainting`** 的 `Faces` 下挂了一批表情（共约 21 个节点，见上文预梳理表）。
2. **目标**：台本 / NodeCanvas / CSV 能选到这些新表情，雅儿村线大立绘能切到对应 `Armor_NoHeadWear_*` 脸。
3. **本期焦点**：
   - 盘点「Prefab 已有节点 vs 枚举已有成员」缺口；
   - 明确哪些必须 `DialogueFaceType` **末尾追加**；
   - `Angry` 是否已可零枚举改动调用；
   - 小头像 / `YaerPainting` Dress / Mask 是否本期必做（默认建议：先大立绘闭环）。
4. **对照**：
   - 0803 Happy：枚举已有、只差 Prefab/资源；
   - 本期多数新表情：Prefab 已挂、**枚举没有**（与古莎 LuoMo 同类卡点）；
   - 古莎 `Awkward` ≠ 雅儿 `GanGa`（名字不同，勿合并）。

---

## 必读 / 优先扫描线索

### A. 枚举与台本
- `Assets/Scripts/Game/Static/Enum/Role/DialogueFaceType.cs`：逐项确认预梳理表「有/无」；当前末尾是否为 `LuoMo`/`LuoMo2`（追加顺序建议写清）
- `StatementNodeEx` / `SayEx` 下拉；CSV `DialogueCsvParser` 非法 FaceType
- 名称必须与后缀一致：`ChiBie2` 不能写成 `ChiBie_2`；禁止建议改 Faces 为中文名

### B. 大立绘
- `GoOutStoryYaerPainting.ResolveGoOutFaceKey`（或等价）：确认键 = `Armor_NoHeadWear_` + `faceType.ToString()`
- Prefab：每个**缺口表情**核实 Image 是否绑图、SizeDelta/Rect 是否异常（Happy/Smug 轮曾有小 Rect 教训）
- 是否存在**同名重复节点**（Smug/Daze 曾重复）——若有，写入风险
- `YaerPainting`（Dress）：是否缺 `Dress_Crown_{新表情}` —— 只列表，默认不本期施工

### C. 小头像
- 四套 `Avatar/Yaer/{Dress|ArmorNone|ArmorCrown|Armor}/` 是否已有对应 PNG
- 缺图时 Loader 行为；默认建议「大立绘先闭环，小头像另补」

### D. 文档
- 0601 / 0614 雅儿表情表是否需扩表（施工建议）

---

## 侦探任务清单

1. **钉死缺口表**（必出；以 Prefab 实扫为准，可修正预梳理）

   | 枚举名 | Prefab 节点 | 枚举有无 | 绑图/Rect | 小头像 PNG | 大立绘最小是否要改代码 | 备注 |
   |--------|-------------|----------|-----------|------------|------------------------|------|
   | …逐行覆盖预梳理全部 21 + 注明原有可跳过详写… | | | | | | |

2. **给出档位结论**  
   - 零代码可调哪些（预期至少含已有枚举+已有正确键的，如部分原 9、Happy、可能 Angry）  
   - 只加枚举即可调哪些  
   - 枚举 + Prefab 校正（Rect/绑图）  
   - 还要补小头像图集的  

3. **枚举追加建议清单**（仅末尾；给出建议追加顺序；说明序列化风险）  
   - 明确：**不要**把 `NanGuo*` 改成复用 `Sad`，**不要**把 `ZhenJing*` 改成复用 `VerySurprised`（节点已按新名挂好）。

4. **最小闭环**  
   - 仅村线 GoOut 大立绘：改哪些文件  
   - 若要小头像：额外步骤  
   - Dress / Mask：写清「另案」

5. **验收清单**  
   - DialogDebug / 含雅儿对话：对每个新枚举选一次 → Hierarchy 只亮对应 `Armor_NoHeadWear_*`  
   - 重点抽测：`Angry`、`RenNai`、`NanGuo`、`ZhenJing`、`ChiBie2`、`GanGa`

6. **开放问题**追加 `Assets/Doc/OPEN_QUESTIONS.md`（新开一节「雅儿立绘 · 批量新增表情 · 2026-08-04」）：  
   - 各新表情中文正式名？  
   - `ChiBie2/3`、`GanGa` 是否与红标 RenNai 段同轮必做？  
   - 小头像四套是否本期？  
   - Dress `YaerPainting` 是否本期？  
   - Mask 是否本期？  
   - `NanGuo*` vs `Sad`、`ZhenJing*` vs `VerySurprised` 策划是否坚持独立频道？

7. **禁止**：改资产；枚举插入中间；建议改节点中文名；扩成全角色改造；把本期写成必须重做 Happy。

---

## 输出要求

写入：`Assets/Doc/执行文档/0804/雅儿立绘批量新增表情_接入表情系统_架构溯源报告.md`

报告结构固定（中文、大白话优先）：

① 结论一句话（哪些必须加枚举、Angry 是否已可调、大立绘最小闭环是什么）  
② 原因（生活类比 + 脚本/Prefab/图集锚点）  
③ 用户需要做什么（拍板清单 + 验收）  
④ 给程序看的补充：
   - 预梳理修正后的完整对照表
   - 须追加枚举名单与建议顺序
   - 施工员最小改动建议（仅 GoOut / +小头像 / +Dress）
   - 与 Happy 轮差异一行
   - 开放问题

完成后用 MASTER 固定四段式口头汇报；详细以报告文件为准。
```

---

## 施工员续跑（侦探报告拍板后再开）

> 本阶段先不要贴。等确认枚举名单 / 小头像范围后再写。

```
@Assets/Doc/00_MASTER_PROMPT.md
@Assets/Doc/执行文档/0804/雅儿立绘批量新增表情_接入表情系统_架构溯源报告.md
@Assets/Doc/OPEN_QUESTIONS.md

你现在是【施工员】。按上述溯源报告做**最小化修改**，使雅儿对话能选中并显示本期拍板的新 FaceType。
优先保持 GoOut 键契约：`Armor_NoHeadWear_{faceType}`；禁止改 Faces 节点为中文；禁止在 Update 堆业务。
新增 DialogueFaceType 成员只能追加在枚举末尾，并说明序列化影响。
每次提交说明：改了哪些文件、实现了什么、如何验证。
```
