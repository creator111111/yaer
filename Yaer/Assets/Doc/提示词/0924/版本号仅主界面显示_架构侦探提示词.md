# Cursor Agent Prompt · 版本号仅主界面显示（撤销局内常驻）

> **角色**：【架构侦探】钉死「主菜单开 / 进局关」挂点 → 【施工员】改 VersionPanel 生命周期  
> **日期**：2026-09-24  
> **产品拍板（覆盖 0922）**：左下角版本号 **`1.0.5`**（`Application.version`）**只在主界面显示**；进局、过场、村庄、战斗、章末、地图等 **一律不显示**  
> **用户证据**：截图左下 `1.0.5`；现网因 0922 施工在局内也常驻  
> **不是**：改 `bundleVersion` 数值；做关于页；挡点击；叠多份；又做成「全游戏常驻」  

**纪律**：对照 0922 报告/施工，明确**撤销**「CloseAll 过滤 + Ensure 重开」的局内常驻策略；Play 验收主菜单有、进村无。

---

## 提示词助手预梳理

### 产品白话

> 主菜单（标题/开始游戏那一屏）左下可以留 `1.0.5`；点进游戏之后就不要再看见这行字。

### 与 0922 的关系（必读，勿再走常驻方案）

| 0922 决议 | 现网实现 | 本票 |
|-----------|----------|------|
| 主菜单 **+ 局内** 常驻 | `VersionPanel` @ System；`CloseAllUIForm` **跳过关闭** + 末尾 `EnsureVersionForm()`；`ProcedureComponentGM` 等处 Ensure | **撤销局内常驻**；仅主界面 |
| 否决「只挂 StartScene」 | 当时怕进局消失 | **现产品就要进局消失** → 生命周期改为「仅 ProcedureMenu / Start 主菜单」 |

### 现网锚点（侦探核实）

| 路径 | 作用 |
|------|------|
| `UI/FormLogic/Version/VersionFormLogic.cs` | 文案=`Application.version` |
| `UIComponentGM.EnsureVersionForm` / `CloseAllUIForm` | 清屏后仍补开 → **局内也有** |
| `ProcedureComponentGM`（约 Ensure 调用） | 开局/流程里打开版本 |
| Prefab `Assets/GameRes/Prefabs/UI/VersionPanel.prefab` | Form 本体 |
| `UIPrefabPath.VersionPanel` | 路径常量 |

### 假说 / 改法方向

| ID | 内容 |
|----|------|
| **P1** | `CloseAllUIForm` 仍跳过 Version + Ensure → 进局清 UI 后版本又回来（主因） |
| **P2** | 仅主菜单 Procedure 打开；`ProcedureGame` / 开始游戏 / 读档进局时 Close Version |
| **P3** | 回主菜单（`ProcedureMenu`）再 Ensure 一次，保证能再看见 |

推荐施工默认：**主菜单打开；进局关闭；CloseAll 不再特殊保留 Version，也不在 CloseAll 末尾 Ensure**（或仅 `ProcedureMenu` 显式 Ensure）。

---

## 侦探（复制给 Agent）

```
你是【架构侦探】。产品覆盖 0922：版本号只在主界面显示，进局后必须消失。

任务：画出 VersionPanel 何时 Open/Ensure/被 CloseAll 保住；给出「仅主菜单」最小改法。

### 必读

@Assets/Doc/执行文档/0922/游戏左下角版本号1.0.5_架构溯源报告.md
@Assets/Doc/施工说明/0922/游戏左下角版本号1.0.5_施工说明.md
@Assets/Scripts/Game/GameRuntime/UI/FormLogic/Version/VersionFormLogic.cs
@Assets/Scripts/Game/GameMgr/Component/UI/UIComponentGM.cs
（EnsureVersionForm / CloseAllUIForm）
@Assets/Scripts/Game/GameMgr/Component/ProcedureComponentGM.cs
@Assets/Scripts/Game/GameRuntime/Procedure/ProcedureMenu.cs
@Assets/Scripts/Game/GameRuntime/Procedure/ProcedureGame.cs
（进局/回菜单事件）

### A. Play 对照表

| 界面 | 现网左下有无 1.0.5 | 产品要求 |
|------|-------------------|----------|
| 主菜单 StartScene | ？ | **要有** |
| 新游戏/读档进局后（村/林） | ？ | **无** |
| ESC 菜单 / 地图 / 对话 | ？ | **无** |
| 回主菜单 | ？ | **再有** |

### B. 调用点清单

列出全部 `EnsureVersionForm` / `OpenUIForm(VersionPanel)` / CloseAll 过滤 Version 的位置。

### C. 方案（报告拍板其一）

| 方案 | 做法 |
|------|------|
| **M1（倾向）** | 删 CloseAll 对 Version 的跳过与末尾 Ensure；仅 ProcedureMenu（或打开主菜单 OpenMainMenu）Ensure；进 ProcedureGame / StartGame 时 Close Version |
| **M2** | 保留 Form，进局把 Version 节点 SetActive(false)，回菜单 true（易漏路径） |
| **否决** | 继续全局常驻；改 bundleVersion；每场景摆死字 |

写入：`Assets/Doc/执行文档/0924/版本号仅主界面显示_架构溯源报告.md`
```

---

## 施工员（侦探报告后再复制）

```
@Assets/Doc/执行文档/0924/版本号仅主界面显示_架构溯源报告.md
@Assets/Scripts/Game/GameMgr/Component/UI/UIComponentGM.cs
@Assets/Scripts/Game/GameMgr/Component/ProcedureComponentGM.cs
@Assets/Scripts/Game/GameRuntime/UI/FormLogic/Version/VersionFormLogic.cs

你是【施工员】。按报告让版本号只在主界面显示。

约束：
- 覆盖 0922「局内常驻」；文案仍读 Application.version（保持 1.0.5），勿改数值除非报告要求
- CloseAll 不再「永久保住 + Ensure」Version（除非报告另有主菜单专用路径）
- 回主菜单必须再能看到版本号
- 不挡点击；不叠多份
- 更新 VersionFormLogic 注释（勿再写「局内常驻」）
- 写入：`Assets/Doc/施工说明/0924/版本号仅主界面显示_施工说明.md`

验收：
- [ ] 主菜单左下可见纯 `1.0.5`（一份）
- [ ] 开始游戏 / 读档进村或进林后左下无版本号
- [ ] 局内 ESC 菜单、地图、对话、章末均无版本号
- [ ] 返回主菜单后版本号再次出现
- [ ] 换场 CloseAll 不会在局内把版本号又 Ensure 出来
```

---

## 使用顺序

1. 「侦探」→ 对照表 + 调用点 + 选 M1/M2  
2. 「施工员」→ 改生命周期；验收主菜单有 / 局内无 / 回菜单再有
