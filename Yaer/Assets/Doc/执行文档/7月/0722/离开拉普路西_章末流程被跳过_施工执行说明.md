# 离开拉普路西 · 章末流程被跳过 — 施工执行说明

**文档版本**：v1.1（2026-07-22）  
**文档性质**：【施工员】基于 `离开拉普路西_章末流程被跳过_架构溯源报告.md` v1.1  
**Unity**：2020.3.48f1  

---

## ① 结论（一句话）

**仓库磁盘上右门本就禁用；本次不改「地图点选进村」主链，只加换场/门触发溯源日志、挡住拉普路西右门误配直跳肯姆尼，并把章末图「重开 MapRight」挪到章末面板打开之后，防止以后再配右门抢跑。**

**v1.1 补充（复测仍无章末）**：上次整图 `JSON.stringify` 曾把 BGM 路径中文弄坏，已按原文恢复并用**字符串级**重排动作；章末触发链补 `[ChapterEnd]` 日志。若进场景即见「已用过，组件禁用」，说明当前存档已写过 `ChapterEndStory_0`，须**新档**或清该剧情记录才能再看演出。

生活类比：正门（地图选关）不动；给后门装了监控摄像头和门锁，并把「先开门再办手续」改成「先办手续再开门」。

---

## ② 改了什么

| 文件 | 实现 |
|------|------|
| `LoadSceneComponentGSM.cs` | `LoadScene` 入口打 `[SceneLoad]`（场景名 + 调用栈） |
| `SceneChangeDoor.cs` | 进门打 `[SceneChangeDoor]`；**拒绝** `WestRappRoad/RightDoor → Village_KenMuNi1` |
| `ChapterEndStory_0.prefab` | 节点4：先章末 UI / `ChapterEndAction` / BGM，**最后**再 `SetObjectActive(MapRight, true)` |
| `WestRappRoad.unity` | **未改**（磁盘已是 `RightDoor` 禁用 + `NextSceneName` 空） |

**禁止项（遵守侦探报告）**

- 未在 `ChapterEndFormLogic` / `ChapterEndAction` 加自动 `LoadScene`
- 未正式启用 `RightDoor` 填村名
- 未按「左门 / 地图误触」去改章末 UI

---

## ③ 复杂逻辑 · 替代方案说明

| 点 | 采用 | 未采用及原因 |
|----|------|--------------|
| R7 定位 | 统一入口 `[SceneLoad]` + 栈 | 只对肯姆尼打日志 → 漏其它误跳 |
| 右门误配 | 运行时拒绝进村 | 只 Warning 仍放行 → 会再次跳过章末 |
| MapRight 时序 | 章末面板后再重开 | 节点4永远不重开 MapRight → 也可能可行（右门本禁用），但保留重开以恢复边界/视觉 |

---

## ④ 如何验证

| # | 步骤 | 期望 |
|---|------|------|
| V1 | Play 前看 `WestRappRoad` → `MapRight/RightDoor` | 组件 **Disabled**，`NextSceneName` **空**（与磁盘一致；不一致先还原再测） |
| V2 | 新档走到拉普路西右缘章末 | 出现章末大标题 → 地图 → 滚动字幕；**不会**自动进村 |
| V3 | Console 过滤 `MapSelect` | 有解锁 /（点选后）点击进村日志 |
| V4 | 字幕结束后点肯姆尼 | 黑幕进 `Village_KenMuNi1`；有 `[MapSelect] 点击…` 与 `[SceneLoad] scene=Village_KenMuNi1` |
| V5 | 若仍「无 MapSelect 却进村」 | 过滤 `SceneLoad` / `SceneChangeDoor`：看栈与门路径，交给验收员钉调用方 |
| V6 | （可选）临时启用 RightDoor 填 `Village_KenMuNi1` 再走进 | 应打 **拒绝换场** Error，**不进村**（测完务必还原禁用+空名） |

---

## ⑤ 提交说明（模板）

```
fix(chapter-end): 防拉普路西右门抢跑并加 SceneLoad 溯源

- LoadScene / SceneChangeDoor 打 [SceneLoad]、[SceneChangeDoor]
- 拒绝 WestRappRoad RightDoor 直跳 Village_KenMuNi1
- ChapterEndStory_0：章末 UI 后再重开 MapRight
```

---

**关联**：`Assets/Doc/执行文档/0722/离开拉普路西_章末流程被跳过_架构溯源报告.md`  
**OPEN_QUESTIONS**：Q1b 仍待验收员核 Editor Hierarchy 是否与磁盘一致。
